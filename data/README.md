# Data

This project uses the Weather benchmark from the official PatchTST/Autoformer dataset source.

The required file for the reproduced experiments is:

```text
weather.csv
```

The CSV contains 52,696 timestamps and 21 weather variables plus the `date` column.
